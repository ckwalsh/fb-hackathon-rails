<% if listing.time_end > Time.now() %>
<h3><%= link_to listing.name, listing %></h3>
<% else %>
<h4><%= link_to listing.name, listing %></h4>
<% end %>
<p>Hacking begins <%= listing.tz_start.strftime('%B %e, %Y') %> at <%= listing.tz_start.strftime('%l:%M %p') %></p>
